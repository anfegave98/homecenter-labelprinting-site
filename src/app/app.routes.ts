import { Routes } from '@angular/router';

/**
 * Rutas de la aplicacion.
 * Las vistas de autenticacion (HU-002) e impresion/historial (HU-009) se incorporan
 * en sus bloques correspondientes; la estructura de carpetas ya esta preparada.
 */
export const routes: Routes = [];
